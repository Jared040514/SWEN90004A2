__pycache__/
*.pyc

# Ignore the partial per-tick replication data captured before we switched to
# --final-only on the full sweep; preserved locally for forensics, not for sharing.
results/full_pertick_partial/

# Local cross-implementation comparison scratch (NetLogo vs Python PNG/SVG
# previews); the corresponding files live in their respective committed locations
# under results/snapshots/ once both branches are merged.
_compare/
